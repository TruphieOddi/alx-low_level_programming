#include "main.h"

/**
 * _isdigit - checks if passed character is a digit or not(0-9)
 * @i: integer to be checked
 *
 * Return: 1 is i is a digit, else 0
 */
int _isdigit(int i)
{
	return (i >= '0' && i <= '9');
}
